# W02 P2 visual evidence

Captured from the phase branch on 2026-09-13 using a temporary copy of the
`offline-recovery` fixture. The scenario recorded a synthetic, non-illustrative
acceptance for the initial snapshot, changed the behavior notes and prototype
CSS, published the later draft, and reloaded the runtime. The record is only
for this evidence capture; it is not product acceptance.

Manual inspection confirmed:

- the unqualified opening resolves to an **Accepted snapshot**, shows its
  provenance, exposes the working draft and comparison links, and reports a
  connected runtime;
- the comparison view presents two material file changes, the prototype asset
  affected by the CSS dependency, and the revision metadata change with old /
  new values and navigation links;
- the retained prototype renders as an isolated mock with its own visual
  warning/controls, and the 740px view keeps the header, proposal views, and
  phase navigation readable.

![Accepted pinned snapshot](accepted-snapshot.png)

![Snapshot comparison](comparison.png)

![Pinned isolated prototype](pinned-prototype.png)

![740px narrow layout](narrow-layout.png)
